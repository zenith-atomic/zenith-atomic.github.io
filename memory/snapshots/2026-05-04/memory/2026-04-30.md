# Memory — Thursday, April 30th 2026

## Project Dragonfly — COMPLETED BUILD

All core components for the production crypto trading bot are built and running in Docker.

### What's running (docker-compose up -d)
| Service | Container | Port | Status |
|---|---|---|---|
| QuestDB | dragonfly-questdb | 9000/9009/8812 | healthy |
| ClickHouse | dragonfly-clickhouse | 8123/9000 | healthy |
| NATS | dragonfly-nats | 4222/8222 | healthy |
| Qdrant | dragonfly-qdrant | 6333/6334 | running (healthcheck curl issue, service itself OK) |
| Neo4j | dragonfly-neo4j | 7687/7474 | healthy |
| Dragonfly cache | dragonfly-cache | 6379 | healthy |
| Bot app | dragonfly-bot | 8000 | running (paper mode) |
| Dashboard | dragonfly-dashboard | 5000 | healthy — http://localhost:5000 |

### Key paths
- Project root: `/home/ai/.openclaw/workspace/project-dragonfly/`
- Docker compose: `project-dragonfly/docker-compose.yml`
- Main orchestrator: `project-dragonfly/main.py`
- Integration tests: `project-dragonfly/tests/test_imports.py`
- Dashboard: `http://localhost:5000`

### Bot config
- Mode: paper trading (no real orders)
- Strategies: `momentum_btc_eth` (enabled), `arb_btc_binance_hyperliquid` (disabled)
- Max drawdown: 20%
- All 6 component modules load successfully inside the container

### Host machine note
- **pip is NOT installed** on this machine (dellrack) and cannot be installed (no root/sudo for apt)
- All Python deps run inside Docker containers only
- Do NOT try to run `pip install -r requirements.txt` on the host

---

## SkillBridge — Premium Redesign (IN PROGRESS)

- SkillBridge live at http://localhost:3080 (auth: demo / skillbridge)
- Subagent for redesign timed out before making changes — needs manual implementation
- Planned: premium mission-control SaaS aesthetic, new color system (deep blacks `#080810`/`#10101a`), JetBrains Mono for metrics, skeleton loading states, 4-metric hero strip
- Files: `skillbridge/public/index.html` (main), `skillbridge/server.js` (API), `skillbridge/SPEC.md`
- New endpoints still needed: `GET /api/metrics`, `GET /api/activity`, `GET /api/calendar/today`, `POST /api/crm/leads/:row/update`
- Subagent session that timed out: `agent:main:subagent:ae13be36-7d9a-4c37-b1cb-21261df42a50`

---

## General
- Session compacted multiple times today due to heavy subagent work
- All 7 subagents completed successfully today (6 Dragonfly components + integration)
- OpenClaw gateway: `ws://127.0.0.1:18789`, Tailscale: `dellrack.taile561c8.ts.net`
